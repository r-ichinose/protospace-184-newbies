<div class="card">
  <%# <%= link_to image_tag("プロトタイプの画像", class: :card__img ), prototypes_path(prototype.id) %> %>
  <div class="card__body">
    <%= link_to @prototype.title, prototypes_path(prototype.id), class: :card__title%>
    <p class="card__summary">
      <%= @prototype.catch_copy %>
    </p>
    <%= link_to "by"@prototypes.user.name, prototypes_path(prototype.id), class: :card__user %>
  </div>
</div>